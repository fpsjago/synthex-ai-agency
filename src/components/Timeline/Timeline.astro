---
const items = [
  {
    year: '2019',
    title: 'Founded in San Francisco',
    description: 'Alex Chen and Sarah Kim leave their jobs at Google DeepMind to start Synthex with a simple thesis: AI is going mainstream, and businesses need expert partners to deploy it responsibly.',
  },
  {
    year: '2020',
    title: 'First enterprise clients',
    description: 'Land first three enterprise clients in logistics and finance. Build our foundational methodology for AI process auditing that we still use today.',
  },
  {
    year: '2021',
    title: 'Series A + team expansion',
    description: 'Raise $4M Series A. Grow from 5 to 24 team members. Launch our LLM integration practice just as GPT-3 changes everything.',
  },
  {
    year: '2022',
    title: '20 clients. $3M saved.',
    description: 'Hit 20 active clients across 8 industries. Our automation systems collectively save clients over $3M in operational costs. Open New York office.',
  },
  {
    year: '2023',
    title: 'The LLM revolution',
    description: 'GPT-4 and Claude launch. We pivot 60% of our practice to LLM integration and agent development. Launch our RAG architecture framework that becomes industry-referenced.',
  },
  {
    year: '2024',
    title: '40 clients. $12M saved.',
    description: 'Cross 40 active clients. Collective savings hit $12M. 98% client retention rate. Named Top 10 AI Agency by AI Business Review.',
  },
];
---

<div class="timeline">
  {items.map((item, i) => (
    <div
      class={`timeline-item ${i % 2 === 0 ? 'timeline-item--left' : 'timeline-item--right'} js-reveal`}
      data-reveal="fade-up"
      data-delay={String(i * 0.1)}
    >
      <div class="timeline-year-wrap">
        <span class="timeline-year label">{item.year}</span>
      </div>

      <div class="timeline-dot">
        <div class="timeline-dot-inner"></div>
      </div>

      <div class="timeline-content">
        <h3 class="timeline-title heading-md">{item.title}</h3>
        <p class="timeline-desc body-sm text-muted">{item.description}</p>
      </div>
    </div>
  ))}

  <!-- Central line -->
  <div class="timeline-line" aria-hidden="true"></div>
</div>

<style>
  .timeline {
    position: relative;
    display: flex;
    flex-direction: column;
    gap: var(--space-3xl);
    padding: var(--space-xl) 0;
  }

  /* Central vertical line */
  .timeline-line {
    position: absolute;
    top: 0;
    bottom: 0;
    left: 50%;
    width: 1px;
    background: linear-gradient(to bottom, transparent, var(--color-cyan), transparent);
    transform: translateX(-50%);
  }

  /* Items */
  .timeline-item {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    gap: var(--space-xl);
    align-items: start;
    position: relative;
    z-index: 1;
  }

  /* Left items: content on left, year on right */
  .timeline-item--left .timeline-year-wrap {
    text-align: right;
    order: 3;
  }

  .timeline-item--left .timeline-content {
    order: 1;
    text-align: right;
  }

  /* Right items: year on left, content on right */
  .timeline-item--right .timeline-year-wrap {
    text-align: left;
    order: 1;
  }

  .timeline-item--right .timeline-content {
    order: 3;
  }

  /* Year */
  .timeline-year-wrap {
    padding-top: 0.5rem;
  }

  .timeline-year {
    font-family: var(--font-mono);
    color: var(--color-text-tertiary);
    font-size: 0.875rem;
  }

  /* Dot */
  .timeline-dot {
    order: 2;
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    margin-top: 0.25rem;
  }

  .timeline-dot-inner {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: var(--color-cyan);
    box-shadow: 0 0 16px var(--color-cyan);
    transition: transform var(--transition-spring);
  }

  .timeline-item:hover .timeline-dot-inner {
    transform: scale(1.5);
  }

  /* Content */
  .timeline-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .timeline-title {
    color: var(--color-text-primary);
  }

  @media (max-width: 768px) {
    .timeline-line {
      left: 16px;
    }

    .timeline-item {
      grid-template-columns: auto 1fr;
      gap: var(--space-md);
    }

    .timeline-item--left .timeline-year-wrap,
    .timeline-item--right .timeline-year-wrap {
      display: none;
    }

    .timeline-dot {
      order: 1 !important;
      margin-top: 0;
    }

    .timeline-item--left .timeline-content,
    .timeline-item--right .timeline-content {
      order: 2 !important;
      text-align: left;
    }
  }
</style>
